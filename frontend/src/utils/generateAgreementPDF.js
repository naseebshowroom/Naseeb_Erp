/**
 * DEPRECATED: PDF generation has moved to server-side using Puppeteer.
 * Use DocumentModal.downloadPDF() instead.
 */
export const printDocument = () => {
  console.warn('printDocument is deprecated. PDF generation is now server-side.');
};
